# План реализации

Источник требований и всех архитектурных решений — [AGENTS.md](AGENTS.md).
Архитектура и оценки ниже унаследованы из `yelex/npa-monitor` (см. `git remote -v`) и
дополнены чек-листом, исполняемым автономным циклом `ralph.sh` (см. правила в конце
файла).

## 1. Архитектура

Три независимых процесса + общая БД:
- **parser/** — cron/systemd timer, ежедневный batch-job: `sources/` (адаптеры под
  источники), `fetcher.py` (httpx+tenacity, retry), `classifier.py` (ключевые слова →
  категория/приоритет), `dedup.py`, `state.py` (доверстывание пропусков).
- **bot/** — aiogram 3 (async, встроенный FSM для многошаговых сценариев: запрос ссылки
  на НПА, причины отклонения), long-running polling-процесс, не вебхук.
- **db/** — SQLAlchemy 2.0 + Alembic, общий слой для parser и bot.

Parser и bot — разные процессы (не один монолит), чтобы падение одного не роняло
другой; связывает их только БД. PDF: PyMuPDF/pdfplumber + pytesseract как fallback.
Docker Compose на VPS.

## 2. БД: SQLite для MVP

Один эксперт, обход раз в сутки, десятки сигналов/день — SQLite в WAL-режиме
достаточен, бэкап = `cp` файла, нет отдельного сервиса БД на VPS. Переход на Postgres
через SQLAlchemy почти бесшовен (расширение на 89 регионов, несколько экспертов,
полнотекстовый поиск).

Таблицы:
- `signals` — карточка сигнала (поля из раздела 7 AGENTS.md)
- `signal_categories` — M2M (сигнал → несколько ЖС)
- `status_history` — аудит переходов (нужно для напоминаний >3 дней)
- `sources_state` — per-source дата последнего успешного обхода (доверстывание)
- `documents_seen` — дедуп независимо от создания сигнала
- `experts` — whitelist

## 3. Переиспользование `measure_deepagent`

Ревью репозитория сделано, см. `docs/STAGE0.md`, раздел 1. Вывод: переиспользуем
отдельные приёмы (2–3 штуки), не архитектуру и не код 1:1:
- многоступенчатый fallback fetcher'а (прямой запрос → прокси → альтернативный ридер) —
  адаптировать приём под наши источники, не копировать код;
- `apparent_encoding`-приём для доменов без charset в заголовках (`garant.ru`,
  `pravo.gov.ru` отдают cp1251 без объявления) — скопировать;
- PDF→текст через `pypdf`/аналог без OCR — взять как baseline, OCR-fallback дописать
  самим (в `measure_deepagent` отсутствует).

Не переиспользуем: полнотекстовый поиск НПА по названию меры (SearXNG/Yandex Search —
решает другую задачу), сам LLM-харнесс экстрактора карточек (это и есть внешний «агент
автообновления», вне периметра), regex-эвристики под конкретные меры/источники (не
обобщаемы), инфраструктуру eval/скоринга (методика оценки — отдельный контур).

`parser/fetcher.py` пишем заново (httpx+tenacity), как и запланировано ниже.

## 4. Белый список источников MVP и сетевые ограничения

Полная таблица со статусом проверки каждого источника — `docs/STAGE0.md`, разделы 2 и
2.1. Кратко:
- `sfr.gov.ru`, `mintrud.gov.ru`, `mos.ru/authority/documents/`,
  `msupport.dszn.ru` — проверены, доступны напрямую.
- `publication.pravo.gov.ru`, `kremlin.ru`, `government.ru` (сеть RSNET) — не отвечают
  на прямые запросы с не-РФ IP на 443/80; рабочая схема — RU-прокси **по HTTP** (443
  фильтруется даже через RU-прокси, 80 — открыт). Подтвердить актуальность схемы на
  боевом VPS перед продакшен-запуском.
- `dszn.ru`/`mos.ru/dszn/` — контент рендерится через JS, нужен Playwright/аналог или
  внутренний API endpoint вместо голого HTTP GET — не блокирует MVP, пока используется
  `mos.ru/authority/documents/` + `msupport.dszn.ru`.
- `r.jina.ai` как fallback-ридер — исключён из стека (не обходит гео-блокировку RSNET).

## 5. Этапы (человеко-дней, один разработчик)

| # | Этап | Оценка | Статус |
|---|---|---|---|
| 0 | Подготовка (ревью measure_deepagent, список источников Москвы, сетевая диагностика) | 1–2 дн | ✅ сделано, см. `docs/STAGE0.md` |
| 1 | БД и общий слой | 1–2 дн | ✅ сделано, коммит `2413e57` (`db/models.py`, `db/enums.py`, `db/service.py`, `db/session.py`, `tests/test_db.py`, 11 тестов) |
| 2 | Parser MVP | 4–6 дн | не начато |
| 3 | Bot MVP | 3–4 дн | не начато |
| 4 | Интеграция / сквозной сценарий | 2 дн | не начато |
| 5 | Деплой | 1–2 дн | не начато |
| 6 | Пилот с экспертом (наблюдение, не разработка) | 1–2 нед | не начато |

Итого разработка (без пилота): **~12–18 человеко-дней**.

## 6. Решения по открытым вопросам

См. AGENTS.md, раздел 16 — статус каждого пункта (🟢/🟡/🔴). Ключевые решения:
- PDF/OCR — fallback-архитектура: текстовый слой основной, OCR (`pytesseract`, ru) —
  только если текст пустой/короткий.
- Дата вступления в силу — regex-эвристики первой итерацией («вступает в силу с…»),
  LLM-разбор вторым проходом, если покрытие низкое по итогам пилота.
- Повторное открытие «Отклонён» — команда `/reopen <id>`, перевод в «Новый» с записью
  в `status_history`.
- Связь с `measure_deepagent` — не жёсткая зависимость, опциональное переиспользование
  fetcher/parser-приёмов как отправной точки (см. раздел 3).

---

## 7. Чек-лист задач (для ralph-цикла)

Каждая задача выполнима за одну итерацию: код + (где применимо) тесты + коммит.

Правила работы с этим разделом (важно для `ralph.sh`/`RALPH_PROMPT.md`):
- Задачи выполняются **сверху вниз**, по одной за итерацию, в рамках текущей
  незавершённой фазы — фазы не пропускаются и не распараллеливаются.
- Выполненная задача помечается `[x]`. Если задача заблокирована (нет доступа/кредов/
  контракта) — оставить `[ ]` и дописать `<!-- BLOCKED: причина -->` рядом, перейти к
  следующей незаблокированной задаче.
- Любое отклонение от AGENTS.md (найдена неточность, нужно уточнение) — фиксировать в
  разделе «Открытые вопросы» AGENTS.md (раздел 16), не изобретать требования на ходу.
- Каждая завершённая задача — отдельный git-коммит с понятным сообщением.

### Фаза 0 — Скелет проекта

- [x] Добавить в `.gitignore` типовые Python-игноры (venv, `__pycache__`, `*.db`,
      `.env`, `ralph_logs/`) поверх уже существующего `_review_measure_deepagent/`.
- [x] Создать структуру пакетов: `parser/` (с `sources/`), `bot/`, `data/`
      (справочники ЖС/регионов) — `db/` и `tests/` уже существуют (Фаза 1).
- [x] `pyproject.toml` создан (Фаза 1, зависимость `sqlalchemy`). Осталось: добавить
      `httpx`, `tenacity`, `aiogram`, `pymupdf`/`pdfplumber`, `pytesseract` по мере
      реализации соответствующих фаз (не всё сразу). Alembic — сознательно не добавлен,
      см. примечание к Фазе 1 ниже.
- [x] Базовый `README.md` с инструкцией запуска для разработчика.
- [x] Настроить `.env.example` (`TELEGRAM_BOT_TOKEN`, `ALLOWED_TELEGRAM_USER_IDS`,
      `GLM_PERSONAL_API_KEY`, `GLM_PERSONAL_BASE_URL`, `GLM_PERSONAL_MODEL`,
      `AUTOUPDATE_AGENT_*`, `RU_PROXY_URL` — заглушки, реальные значения не коммитить).
- [x] CI: `.github/workflows/ci.yml` (`ruff check .` + `pytest` на push/PR в `main`),
      `ruff` добавлен как dev-зависимость, конфиг в `pyproject.toml`
      (`[tool.ruff]`/`[tool.ruff.lint]`). Существующий код Фазы 1 прогнан через
      `ruff --fix` (сортировка импортов, лишние кавычки в forward-ref, `typing.Iterator`
      → `collections.abc.Iterator`) — поведение не менялось, тесты остались зелёными.

**Фаза 0 завершена.**

### Фаза 1 — БД и общий слой (`db/`) — ✅ сделано (коммит `2413e57`)

- [x] Модели SQLAlchemy 2.0: `signals`, `signal_categories`, `status_history`,
      `sources_state`, `documents_seen`, `experts` (раздел 2 выше и раздел 7 AGENTS.md).
      См. `db/models.py`, `db/enums.py`.
- [x] Инициализация схемы через `Base.metadata.create_all` (`db/session.py`), SQLite
      WAL. **Решение:** Alembic-миграции сознательно отложены за пределы MVP (схема
      пока меняется целиком через `create_all`, не через миграции) — не пробел, а
      осознанное упрощение, зафиксированное в докстринге `db/session.py`. Вернуться к
      этому, когда схема стабилизируется или потребуется production-миграция без
      потери данных.
- [x] CRUD-слой сигналов: `db/service.py` — `create_signal`, `transition_status` (с
      валидацией допустимых переходов по `ALLOWED_TRANSITIONS`, включая `/reopen`:
      Отклонён → Новый), `register_document_seen` (дедуп), `update_source_state`
      (доверстывание). Выборки по фильтрам `/today /pending /history /stats` — ещё не
      реализованы, добавить в Фазе 5 (Bot MVP) вместе с самими командами.
- [x] Дедупликация через `documents_seen` (по `doc_url`, уникальный констрейнт).
- [x] Юнит-тесты слоя БД: `tests/test_db.py`, 11 тестов, зелёные
      (`python -m pytest tests/test_db.py` — проверено при слиянии).

### Фаза 2 — Справочники (данные, не код)

- [x] Формат справочника ЖС (`data/life_situations.yaml`): id, название, ключевые слова
      по разделу 5 AGENTS.md. Заполнены 3 стартовые ЖС.
- [x] Формат справочника регионов (`data/regions.yaml`): код субъекта, название, домены
      источников. В MVP заполнено Москвой + «РФ», формат рассчитан на все 89 субъектов.
- [x] Загрузчик справочников — `db/catalog.py` (не в БД, а в памяти при обращении: YAML →
      dataclass'ы `LifeSituation`/`RegionEntry`/`Source`). **Уточнение относительно
      исходной формулировки задачи:** в БД (Фаза 1) нет таблиц под ЖС/регионы — там это
      фиксированные Python-enum'ы `SignalCategory`/`Region` (осознанное ограничение MVP,
      см. AGENTS.md раздел 16). Загрузчик валидирует id/code справочника против этих
      enum'ов и явно падает `CatalogError` при рассинхронизации — значит, «без изменения
      кода» пока честно работает для ключевых слов/источников внутри уже существующей
      ЖС/региона, но не для добавления совершенно нового вида ЖС/региона (это по-прежнему
      требует строку в enum). Тест на подхват изменений — `tests/test_catalog.py`
      (8 тестов, включая happy path и оба сценария рассинхронизации).
- [x] Белый список доменов — `data/sources.yaml` (federal/other) + `data/regions.yaml`
      (региональные), с пометкой способа доступа (`direct`/`ru_proxy`/`unsupported`).
      Объединённый список — `db/catalog.py::all_domains()`.
- [ ] (Опционально, вне MVP) скрипт-краулер `scripts/build_region_sources.py` по
      `http://pravo.gov.ru/links/region/` — черновик справочника для расширения за
      пределы Москвы. Не блокирует MVP-фазы, отложен после Этапа 4 (как и было решено).

**Фаза 2 завершена** (кроме опционального краулера, вне MVP).

### Фаза 3 — Parser MVP (`parser/`)

- [x] `fetcher.py`: httpx+tenacity, ретраи (3 попытки → `SourceUnavailable`), таймауты,
      автоопределение кодировки (`charset_normalizer`), схема прямой-запрос/через-
      `RU_PROXY_URL` по `access` из справочника (`db/catalog.py::Source`). Браузероподобный
      User-Agent — mintrud.gov.ru отдаёт 403 на идентифицирующий UA (см. docs/STAGE0.md).
      5 тестов (`tests/test_fetcher.py`, без реальной сети — `httpx.MockTransport`).
- [x] Адаптер `sources/sfr.py` (`fetch_news`), `sources/mintrud.py` (`fetch_docs`) —
      реализованы и протестированы на реальной вёрстке, снятой вживую 2026-08-19 (не на
      придуманной разметке). **Уточнение к `docs/STAGE0.md`:** раздел нормативных актов
      sfr.gov.ru (`/about/prf_order/` → `/order/`) на практике оказался категорийным
      индексом подстраниц (напр. `/order/voennoslujaschim`), а не плоским листингом —
      исправлено в `docs/STAGE0.md`, сканирование подстраниц не реализовано в этой
      итерации (отдельная задача на будущее, `sources/sfr.py` пока покрывает только
      `press_center/news/`). 4 теста (`tests/test_sources_sfr.py`,
      `tests/test_sources_mintrud.py`) на реальных фрагментах вёрстки.
- [x] Адаптер `sources/pravo_gov.py` (`fetch_documents`, AJAX `/Documents/search`),
      `sources/kremlin.py` (`fetch_news`), `sources/government.py` (`fetch_docs`) — все
      на `access="ru_proxy"`, реализованы и протестированы на реальной вёрстке. См.
      `docs/STAGE0.md` раздел 4 для деталей находок (AJAX-эндпоинт pravo.gov.ru,
      HTTPS-таймаут government.ru). Схему `RU_PROXY_URL` всё ещё нужно подтвердить на
      боевом VPS — доступность из среды разработки не гарантирует то же на проде.
- [x] Адаптер `sources/mos_ru.py` (`fetch_documents`, парсинг JSON `__NEXT_DATA__`,
      устойчивее CSS-селекторов с build-хешем) и `sources/msupport_dszn.py`
      (`fetch_news`, листинг `/news`, не главная страница) — региональные источники MVP
      (Москва). Реализованы и протестированы на реальной вёрстке.
- [x] Адаптер «иных» источников — `sources/other.py::fetch_feed`/`fetch_all`: RSS для
      `garant.ru`, `consultant.ru`, `tass.ru`, `ria.ru`, `rg.ru` (все проверены вживую,
      URL — реальные RSS-эндпоинты, не главные страницы). `docs.cntd.ru` — сетевой
      недоступен (таймаут, известная проблема, `access: unsupported` в
      `data/sources.yaml`), не реализован. 4 теста (`tests/test_sources_other.py`).
- [x] PDF-парсинг: `parser/pdf.py::extract_text` — PyMuPDF (`fitz`) как основной слой,
      `pytesseract`-fallback (lang=`rus`) если текст страницы короче
      `MIN_PAGE_TEXT_LENGTH`. `pdfplumber` не использован — PyMuPDF даёт и текст, и
      рендер страницы в изображение для OCR одним пакетом. Тесты мокают OCR-вызов —
      бинарника `tesseract` нет в среде разработки/CI (нужен на боевом VPS, см. Фазу 7).
      4 теста (`tests/test_pdf.py`).
- [x] `parser/state.py::fetch_window_start`/`mark_source_processed` — хранение даты
      последнего успешного обхода per-источник через `db.models.SourceState`,
      доверстывание пропущенных периодов (не жёстко «последние 24 часа»). 4 теста
      (`tests/test_state.py`).
- [x] `parser/filters.py::is_domain_whitelisted` — проверка домена (с поддоменами) по
      белому списку, `is_text_content` — фильтр по типу контента (HTML/plain/PDF).
      7 тестов (`tests/test_filters.py`).

**Побочная находка при разработке `state.py`:** SQLite через SQLAlchemy теряет
`tzinfo` при чтении даже для колонок `DateTime(timezone=True)` — затрагивало всю схему
Фазы 1, не только новый код. Исправлено в `db/types.py::UTCDateTime` (см. отдельный
коммит) — все datetime-колонки `db/models.py` переведены на этот тип, старый тест
`tests/test_db.py::test_source_state_upsert`, ранее подстроенный под баг, приведён в
соответствие с исправленным поведением.

**Фаза 3 завершена.**

### Фаза 4 — Классификация и формирование сигнала

- [x] `classifier.py`: keyword-фильтр по разделу 5 AGENTS.md (ЖС + тематический блок),
      релевантность — строго ЖС ∩ тематический блок (раздел 4.4). Ключевые слова —
      `data/keywords.yaml` (новый справочник, загрузчик в `db/catalog.py`).
      **Известное ограничение:** точные подстроки без лемматизации — см. AGENTS.md
      раздел 16, пункт 13.
- [x] Определение типа события по тексту (`detect_event_type`) — маппинг
      ключевое-слово→`EventType` составлен реализатором (прямого маппинга в AGENTS.md
      нет), см. докстринг `parser/classifier.py`. «Тип документа» отдельно не
      определяется — в карточке сигнала (AGENTS.md раздел 7) такого поля нет; маркеры
      документа (5.3) используются только для приоритизации (см. ниже).
- [x] Определение региона (`detect_region`): домен источника → «РФ» для федеральных →
      «Не определён». Текстовое уточнение не реализовано — не нужно, пока в справочнике
      всего 2 региона (см. докстринг функции и AGENTS.md раздел 16, пункт 12).
- [x] Приоритизация (`detect_priority`) по правилам раздела 7 AGENTS.md. «Есть ссылка
      на документ» трактуется как «есть маркер документа (5.3) в тексте» — см. докстринг
      `parser/classifier.py` для обоснования этого и других решений по неоднозначным
      формулировкам ТЗ.
- [x] Извлечение даты вступления в силу — `parser/effective_date.py::extract_effective_date`:
      явная дата после «вступает/вступил/вступила в силу», либо дата публикации при
      формулировке «со дня официального опубликования». Общий разбор русских дат
      вынесен в новый `parser/ru_dates.py` (был продублирован в `sources/mintrud.py` и
      `sources/msupport_dszn.py` — рефакторинг отдельным коммитом). 6 тестов
      (`tests/test_effective_date.py`) + 6 тестов на сам `ru_dates.py`.
- [x] Интерфейс `ClassifierLLMClient` + реализация `GLMClient` — `parser/llm.py`. Без
      LangChain (прямой HTTP-вызов через `httpx`, OpenAI-совместимый эндпоинт
      api.z.ai) — единственный нужный вызов не оправдывает тяжёлую зависимость
      `langchain-openai`. `get_default_client()` возвращает `None`, если
      `GLM_PERSONAL_API_KEY` не задан — вызывающий код трактует это как «LLM
      недоступен», не как ошибку (LLM необязателен для MVP). 5 тестов
      (`tests/test_llm.py`, без реального обращения к GLM).
- [x] Сборка объекта «сигнал» — `parser/signals.py::build_signal`: релевантная
      публикация → `db.service.create_signal` (статус «Новый»); нерелевантная →
      `None` (сигнал не создаётся, не отклоняется — отклонение это ручное действие
      эксперта). Дедупликация — забота оркестратора (Фаза 6), не этой функции.
      2 теста (`tests/test_signals.py`).
- [x] Тесты классификации на синтетических примерах публикаций (по каждой ЖС, по
      каждому уровню приоритета) — `tests/test_classifier.py`, 17 тестов.

**Фаза 4 завершена.**

### Фаза 5 — Bot MVP (`bot/`)

Основной код (`bot/main.py`, `config.py`) пришёл слиянием из ветки `phase4-classifier`
(своя, параллельная реализация Фазы 4/5 — см. merge-коммит и AGENTS.md раздел 16,
дополнено про альтернативный classifier.py). При интеграции найдено и исправлено
несколько реальных багов в перенесённом коде и в уже смерженной Фазе 1 — см. отдельные
`fix(db)`/`fix(bot)` коммиты сразу после merge-коммита.

- [x] Инициализация aiogram 3, allowlist Telegram user id (`ALLOWED_TELEGRAM_USER_IDS`),
      игнор сообщений от посторонних (`is_allowed()` в каждом хендлере), логирование
      всех переходов статуса (добавлено при интеграции — было только 1 лог-вызов на
      весь модуль).
- [x] Утренняя сводка — `cmd_digest`, группировка по приоритету (`PRIORITY_ORDER`),
      «Новых сигналов нет» при пустой выборке. Отправка по расписанию (~08:00) — Фаза 6,
      здесь только сама команда/логика.
- [x] Карточка сигнала (`signal_card`) со всеми полями и inline-кнопками ✅/❌/↩️
      (`signal_kb`).
- [x] FSM-сценарий ✅ Взять в работу → запрос ссылки на НПА → проверка доступности →
      «✅ Ссылка принята» → статус «Передан агенту» (`on_signal_button`/`on_npa_link`).
      **Проверка доступности и белый список домена отсутствовали в перенесённом коде**
      (принимался любой текст, начинающийся с `http`) — добавлены при интеграции через
      `parser/fetcher.py`/`parser/filters.py`, сетевой вызов — до открытия DB-сессии.
- [x] FSM-сценарий ❌ Отклонить → выбор причины из 4 вариантов → статус «Отклонён»
      (`on_reject_reason`). **Баг при интеграции:** передавался `reason=` вместо
      `rejection_reason=` — `transition_status` падал с ValueError на каждой попытке
      отклонить сигнал; исправлено.
- [x] Обработчик ↩️ Позже → статус «Отложен» (`on_signal_button`, action `later`).
      **Баг в уже смерженной Фазе 1:** `db/service.py::ALLOWED_TRANSITIONS` не разрешал
      Новый→Отложен (и Отложен→Отклонён) — оба перехода прямо в AGENTS.md раздел 6;
      исправлено отдельным `fix(db)` коммитом, 2 новых теста в `tests/test_db.py`.
- [x] Команда `/reopen <id>` для отклонённых сигналов → статус «Новый».
- [ ] Адаптер `AutoUpdateAgentClient` (интерфейс) для передачи ссылки + `measureId`
      внешнему агенту автообновления.
      <!-- BLOCKED: агент автообновления — это `/Users/user/dev/auto` (LangGraph-пайплайн
      `product_agent`), но НЕ готовый API: HTTP-роуты монтируются только внутри
      проприетарного Sberbank-рантайма `dab2c_aef_framework`, которого в репозитории нет.
      Есть рабочий скрипт `run_offline_pipeline.py` (принимает текст документа, не URL).
      Нужно решение пользователя перед этой задачей: (а) вызывать как библиотеку/
      сабпроцесс `run_offline_pipeline.py` локально, или (б) ждать реально задеплоенный
      Sberbank-эндпоинт и интегрироваться по HTTP. См. AGENTS.md раздел 16, пункт 8. -->
      — до решения `on_npa_link` только логирует `npa_link`, реальной отправки нет.
- [x] Команды `/today`, `/pending`, `/history` (последние 10), `/stats` (агрегация за
      неделю).
- [x] Напоминание о сигналах >3 дней в статусе «В работе» — `remind_stale`/
      `_reminder_loop` (раз в 6 часов). **Баг при интеграции:** сравнение naive
      `datetime.now()` с tz-aware полями БД (после `db/types.py::UTCDateTime`, Фаза 3)
      — `ValueError` при каждом вызове; исправлено (`dt.datetime.now(dt.timezone.utc)`),
      как и аналогичная строка в `cmd_stats`.
- [x] Команда `/complete <id>` — «Передан агенту» → «Завершён» после проверки экспертом
      результата агента. Ручная команда (не автоматический переход) — до подключения
      реального `AutoUpdateAgentClient` нет события «агент ответил», которое могло бы
      триггерить переход автоматически.
- [x] `bot/__main__.py` — докстринг `bot/main.py` обещал `python -m bot`, но у пакета не
      было `__main__.py`; добавлено.

19 тестов `tests/test_bot_main.py` (хендлеры вызываются напрямую с моками
Message/CallbackQuery/FSMContext — без реального `Bot`/`Dispatcher`), включая
регрессионные тесты на все перечисленные выше баги.

**Фаза 5 завершена** (кроме `AutoUpdateAgentClient` — заблокирован решением пользователя
по стратегии интеграции с `/Users/user/dev/auto`, см. AGENTS.md раздел 16 п.8).

### Фаза 6 — Интеграция и планировщик

- [x] `parser/orchestrator.py` — связывает `fetcher`/`state`/`filters`/`classifier`/
      `signals` в один прогон по всем источникам; `parser/__main__.py` — entrypoint
      `python -m parser`. Ежедневный запуск в 06:00 — через cron/systemd timer, пример
      crontab-строки в README. **Живой смок-тест** (без мока, реальные источники) нашёл
      и исправил баг: `process_source` ловил только `SourceUnavailable`, а неверно
      настроенный `RU_PROXY_URL` (`ValueError`) ронял весь прогон целиком — исправлено,
      9 тестов (`tests/test_orchestrator.py`).
- [x] Отправка утренней сводки ~08:00 — `bot/main.py::_digest_loop`/`send_digest`,
      фоновая задача внутри самого бота (не отдельный cron), аналогично уже имевшемуся
      `_reminder_loop`. Логика подбора сигналов вынесена в `_digest_signals`, общая с
      командой `/digest`.
- [x] Сквозной прогон happy path (раздел 11 AGENTS.md) — `tests/test_happy_path.py`:
      парсер создаёт сигнал → сводка → ✅ Взять в работу → ссылка на НПА → «Передан
      агенту» → `/complete` → «Завершён», с проверкой полной истории переходов и того,
      что сигнал корректно уходит из `/today` в `/history`. Все шаги — через реальные
      `parser`/`bot` модули на одной SQLite, не мок внутренней логики (только сеть).
- [x] Проверка граничных случаев (раздел 12 AGENTS.md) — покрыты тестами по всему
      проекту (источник недоступен/3 попытки — Фаза 3; дубликат публикации — Фаза 6
      орк-тор; ссылка на НПА недоступна — Фаза 5; сигнал >3 дней в работе — Фаза 5;
      «Позже» → в следующей сводке и пустая сводка — добавлено здесь,
      `tests/test_bot_main.py`).

**Побочная находка при интеграции:** `db.query(Signal)` в `cmd_today`/`cmd_pending`/
`cmd_history`/`_digest_signals` не делал `selectinload(Signal.categories)` — обращение
к `.categories` после закрытия сессии (в `_send_list`/карточке) падало с
`DetachedInstanceError`. Ни один прежний тест не гонял «счастливый путь» этих команд с
непустым результатом, поэтому баг не был замечен раньше. Исправлено во всех четырёх
местах, 9 регрессионных тестов.

**Фаза 6 завершена.**

**Доработка по запросу пользователя (2026-08-20):** трейс парсинга — `python -m parser
-v` (`parser/__main__.py`) выводит по каждой публикации, какие ключевые слова ЖС/
тематического блока совпали, итоговую релевантность, тип события/регион/приоритет и
id созданного сигнала (если создан); DEBUG-уровень ограничен только модулями `parser.*`,
чтобы не тонуть в логах httpx/httpcore. Добавлены `Classifier.explain()` +
`ClassificationTrace` (`parser/classifier.py`) и `parser.ru_stem.find_matches` — какие
именно ключевые слова совпали, не только факт совпадения. Заодно исправлена мелкая
неэффективность в `process_source`: при достижении публикации старше окна поиска
перебор списка (внутри одной страницы) не останавливался сразу — обрабатывал/логировал
все оставшиеся элементы страницы вместо немедленной остановки (публикации в листингах
идут от новых к старым, дальше проверять нечего). 1 регрессионный тест на это плюс
тесты на `explain()`/`find_matches`.

### Фаза 7 — Деплой

- [ ] `Dockerfile`(ы) для parser и bot.
- [ ] `docker-compose.yml`: сервисы + volume для SQLite-файла + переменные окружения из
      `.env`.
- [ ] Настройка `RU_PROXY_URL` на боевом VPS, подтверждение схемы доступа к RSNET-доменам
      (раздел 4 выше).
- [ ] Документация по разворачиванию на VPS (README, раздел «Deploy»).

### Фаза 8 — Проверка критериев готовности

Сверка с разделом 15 AGENTS.md — каждый пункт закрывается тестом или ручным сценарием:

- [ ] Парсер обходит источники по расписанию и создаёт сигналы.
- [ ] Бот присылает сводку в заданное время.
- [ ] Эксперт может взять в работу / отклонить / отложить / переоткрыть сигнал.
- [ ] Бот принимает и проверяет ссылку на НПА.
- [ ] Бот передаёт ссылку и `measureId` агенту (через адаптер, даже если адаптер —
      пока заглушка).
- [ ] Добавление новой ЖС и нового региона проверено end-to-end через справочник, без
      изменений кода.
- [ ] `/today /pending /history /stats` работают на реальных данных.
- [ ] Все граничные случаи раздела 12 AGENTS.md покрыты тестами или ручной проверкой.

## Открытые внешние зависимости (не решаются кодом)

- Контракт API агента автообновления.
- Креды и пример вызова GLM.
- Файл методики оценки качества (справочно, не блокирует).
- Доступ к боевому VPS и подтверждение сетевой схемы (RU-прокси) в реальных условиях.
